<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External files directory -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Cache directory for sharing -->
    <cache-path
        name="shared_audio"
        path="." />

    <!-- External paths -->
    <external-path
        name="dcim"
        path="DCIM/" />

    <external-path
        name="downloads"
        path="Download/" />

    <!-- Root external storage (if needed) -->
    <external-path
        name="external"
        path="." />
</paths>